% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip-int.R
\name{perpsnip}
\alias{perpsnip}
\title{PerpetualBooster via parsnip}
\usage{
perpsnip(
  mode = "classification",
  engine = "perpetual",
  objective = NULL,
  budget = NULL,
  iteration_limit = NULL,
  stopping_rounds = NULL,
  max_bin = NULL,
  num_threads = NULL,
  missing = NULL,
  allow_missing_splits = NULL,
  create_missing_branch = NULL,
  missing_node_treatment = NULL,
  log_iterations = NULL,
  quantile = NULL,
  reset = NULL,
  timeout = NULL,
  memory_limit = NULL,
  seed = NULL,
  calibration_method = NULL,
  save_node_stats = NULL
)
}
\arguments{
\item{mode}{A string for the model mode. One of "classification",
"regression", or "censored regression".}

\item{engine}{A string for the engine. Currently only "perpetual".}

\item{objective}{A string specifying the objective function. Default is "LogLoss".}

\item{budget}{A numeric value ensuring the training time does not exceed this budget (in normalized units).}

\item{iteration_limit}{An integer limit on the number of iterations.}

\item{stopping_rounds}{An integer for early stopping.}

\item{max_bin}{Integer, max number of bins for histograms.}

\item{num_threads}{Integer, number of threads to use.}

\item{missing}{Value to consider as missing data. Default is NaN.}

\item{allow_missing_splits}{Boolean.}

\item{create_missing_branch}{Boolean. Whether to create a separate branch for missing values (ternary trees).}

\item{missing_node_treatment}{String. How to handle weights for missing nodes if create_missing_branch is True. Options: "None", "AssignToParent", "AverageLeafWeight", "AverageNodeWeight".}

\item{log_iterations}{Integer.}

\item{quantile}{Numeric.}

\item{reset}{Boolean.}

\item{timeout}{Numeric.}

\item{memory_limit}{Numeric.}

\item{seed}{Integer seed for reproducibility.}

\item{calibration_method}{String specifying the calibration method for prediction intervals. Options: "WeightVariance", "MinMax", "GRP", "Conformal".}

\item{save_node_stats}{Boolean. Whether to save node statistics (required for some calibration methods and importance types).}
}
\value{
A \code{model_spec} object of class \code{perpsnip}.
}
\description{
A parsnip-compatible model spec for the PerpetualBooster algorithm.
Supports classification, regression, and censored regression modes.
}
\examples{
\dontrun{
library(perpsnip)
library(workflows)

spec <- perpsnip(mode = "classification", budget = 0.5)

wf <- workflow() |>
  add_formula(am ~ .) |>
  add_model(spec) |>
  fit(data = dplyr::mutate(mtcars, am = as.factor(am)))
}

}
